<div class="block round grid100">
    <div class=" fl">
        <%= link_to image_tag(product.image.picture_url, :class=>"product_small_img_box") if product.image && product.image.picture_url %>
        <%= product.name %>
    </div>
</div>