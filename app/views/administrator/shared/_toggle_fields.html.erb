<span class='toggle <%= field %> <%= item[field] ? "on" : "off" %>'>
    <%= link_to image_tag("toggle_transparent.png"), administrator_base_toggle_path(:id => item.id, :type => item.class.name, :field => field), :method => :put, :remote => true, :title => "toggle_link" %>
</span>