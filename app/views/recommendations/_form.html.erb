<%= form_for @recommendation, :html => { :multipart => true } do |f| %>
    <%= render "shared/error_messages", :target => @recommendation %>

    <%= render "info_items/basic_fields", :item => @recommendation, :f => f %>

    <div class="actions">
        <%= f.submit t("general.submit"), :class => "button" %>
    </div>
<% end %>

<%= render "info_items/delay_load_stuff", :item => @recommendation %>
