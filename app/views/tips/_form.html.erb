<%= form_for(@tip) do |f| %>
  <%= render "shared/error_messages", :target => @tip %>

  <%= f.hidden_field :type  %>
  <%= f.hidden_field :title %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.label f.object.title %>
  </div>
  <div class="field">
    <%= f.label "content" %><br />
    <%= text_field_tag "content", f.object.current_content %>
  </div>
  <div class="actions">
    <%= f.submit t("general.submit") %>
  </div>
<% end %>
