<div class="content_block_inside">
  <h2><%= t("groups.all") %></h2>
    <% @groups.each do |group| %>
        <p><%= link_to group.name, group %>(<%= group.members_count.to_s %><%= t("groups.member") %>)</p>
    <% end %>
  <br/>
  <%= link_to t("groups.new"), new_group_path, :class => "button" %>
</div>