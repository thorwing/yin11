$(document).trigger('close.facebox');
$('#current_city_name').text('<%= current_city.name %>');
var city_input = $('.map_city');
if(city_input){
    city_input.val('<%= current_city.name %>');
}

//TODO
//var vendors_list = $("#vendors_list");
//if(vendors_list.length > 0) {
//    vendors_list.empty();
//
//}


update_map(true);
