<div>
    <%= comment.user.login_name %> :  <blockquote> <%= comment.content %></blockquote>
    <div class="actions">
        <%= link_to "Reply", home_new_comment_path(:id => item.id, :type => item.class.name, :parent_comment_id => comment.id) %>
    </div>
</div>