<b><%= target.vendor.name %></b>
<%= hidden_field_tag "#{target.class.name.downcase}[vendor_token]", target.vendor.id %>
<!--for testing -->
<%= hidden_field_tag "#{target.class.name.downcase}[vendor_name]", target.vendor.name %>
