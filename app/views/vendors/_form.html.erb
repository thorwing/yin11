<%= form_for(@vendor) do |f| %>
    <%= render "shared/error_messages", :target => @vendor %>
    <div class="field">
        <b><%= f.label :name, t("general.name") %></b><%= mark_required(@vendor, :name) %><br/>
        <%= f.text_field :name, :maxlength => get_max_length(@vendor, :name) %>
    </div>

    <div class="field">
        <b><%= f.label :category, t("general.category") %></b><br/>
    </div>




    <% if current_user_has_permission?(:administrator) %>
        <div class="field">
            <%= f.label :enabled, "enabled" %><br />
            <%= f.check_box :enabled, {}, true, false %>
        </div>
    <% end %>

    <div class="actions">
        <%= f.submit t("general.submit"), :class => "button" %>
    </div>
<% end %>
